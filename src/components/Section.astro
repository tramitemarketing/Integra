---
// src/components/Section.astro
interface Props { alt?: boolean; class?: string; [key: string]: unknown; }
const { alt = false, class: cls = '', ...rest } = Astro.props;
---
<section class={`section ${alt ? 'section--alt' : ''} ${cls}`} {...rest}>
  <div class="container"><slot /></div>
</section>
<style>
  .section { padding-block: var(--space-24); }
  .section--alt { background: var(--bg-alt); }
  @media (max-width: 640px) { .section { padding-block: var(--space-16); } }
</style>
